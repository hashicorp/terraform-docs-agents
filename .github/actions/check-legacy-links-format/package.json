{
  "name": "check-legacy-links-format",
  "version": "0.0.1",
  "description": "Checks if MDX links in a PR's file diff are not in the correct format for dev-portal",
  "main": "index.js",
  "author": "Ashlee Boyer",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@vercel/ncc": "^0.36.1",
    "remark": "^12.0.1",
    "unist-util-visit": "^2.0.3"
  },
  "scripts": {
    "build": "ncc build index.js -o dist"
  }
}
